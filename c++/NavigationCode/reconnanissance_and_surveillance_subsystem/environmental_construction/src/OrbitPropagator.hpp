#ifndef ORBIT_PROPAGATOR_HPP
#define ORBIT_PROPAGATOR_HPP

#include <iostream>

class OrbitPropagator
{
public:
    OrbitPropagator(){}
    ~OrbitPropagator(){}

    void propagateOrbit()
    {
        // 轨道传播逻辑
        // 这里可以添加具体的轨道传播算法实现
        std::cout << "正在进行轨道传播..." << std::endl;
    }
};

#endif // ORBIT_PROPAGATOR_HPP